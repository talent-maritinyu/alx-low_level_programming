#include "main.h"
/**
 * int _abs - computes the absolute value of an integer
 *@r: the integer to check
 * Return: Absolute value r
 */
int _abs(int);
{
	int r ;

	r = 0;

	if (r < 0)
	{
		_putchar('absValue =' " -%d\n", r);
	}
	else
	{
		_putchar('absValue =' " %d\n", r);
	}
       return (0);
}       
