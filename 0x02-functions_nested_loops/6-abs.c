#include "main.h"
/**
 * _abs - computes the absolute value of an integer r
 *@r: the integer to check
 * Return: Absolute value r
 */
int _abs(int r)
{
	if (r < 0)
		return (-r);
	else 

	 return (r);
}       
