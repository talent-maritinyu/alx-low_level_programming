#include "main.h"
/**
 * add - adds two integers
 * @a: int to be added to b
 * @b: int to be added to a
 * Return: returns the result
 */
int add(int, int)
{
	int a, b;
	int result;

	result = a + b;
	return (result);
}
