#include "main.h"
/**
 * print_to_98 - prints all natural numbers from n to 98
 * Return: Always
 */
void print_to_98(int n)
{
	int i;

	for (n = i; n <= 98; n++)
		if (n !=98)
			_putchar(',);
	_putchar(' ');
	_putchar('\n);
	return (0);
}
