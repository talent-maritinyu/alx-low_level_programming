#include <stdio.h>
/**
 * main - Entry point print alphabet in lowercase
 *
 * Return: Success
 */
int main(void)
{
	char c;

	for (c = 'z'; c >= 'a'; c--)
		putchar(c);
	purchar("\n");
	return (0);
}
